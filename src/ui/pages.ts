export function escapeHtml(value: unknown): string {
  return String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[char]!);
}
export interface PageContext { flow: string; csrf: string; clientName: string; scope: string; error?: string; notice?: string; accessMode?: 'read' | 'readwrite'; selectedAccountIds?: string[] }
export interface AdAccountChoice { id: string; name: string; currency?: string; account_status?: number }
export interface SavedConnection { clientId: string; clientName: string; accountCount: number }
const h = escapeHtml;
function hidden(context: PageContext): string {
  return `<input type="hidden" name="flow" value="${h(context.flow)}"><input type="hidden" name="csrf" value="${h(context.csrf)}">`;
}
function page(title: string, content: string, step: number, clientName = 'Seu assistente'): string {
  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light"><title>${h(title)} | FaceAds</title><style>
:root{--navy:#10223b;--blue:#205de6;--paper:#f3f6fa;--muted:#56667b;--line:#dfe6ef;--white:#fff;--danger:#a82130}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--navy);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;font-size:15px;line-height:1.55}a{color:var(--blue)}.shell{max-width:1000px;margin:0 auto;padding:32px 24px 44px}.brand{font-size:23px;font-weight:760;letter-spacing:-1px;display:flex;align-items:center;gap:10px;margin-bottom:36px}.mark{display:grid;place-items:center;background:var(--blue);color:white;border-radius:9px;width:34px;height:34px;font-size:13px;letter-spacing:0}.brand small{margin-left:auto;font-size:12px;letter-spacing:.04em;font-weight:600;color:var(--muted)}.layout{display:grid;grid-template-columns:250px minmax(0,1fr);min-height:550px;background:white;border:1px solid var(--line);border-radius:18px;overflow:hidden;box-shadow:0 18px 50px #10223b08}.rail{background:var(--navy);color:#fff;padding:34px 26px;display:flex;flex-direction:column;gap:30px}.eyebrow{font-size:11px;letter-spacing:.12em;font-weight:650;text-transform:uppercase;opacity:.72}.client{font-size:24px;font-weight:650;line-height:1.25;margin-top:10px;overflow-wrap:anywhere}.rail p{color:#c3cfdf;font-size:13px}.steps{list-style:none;margin:0;padding:0;display:grid;gap:23px}.steps li{display:flex;align-items:center;gap:12px;font-size:14px;color:#a7b7cb}.steps span{height:27px;width:27px;flex-shrink:0;display:grid;place-items:center;border:1px solid #526178;border-radius:50%;font-size:12px}.steps .active{color:#fff;font-weight:650}.steps .active span{background:#fff;color:var(--navy);border-color:#fff}.rail-foot{margin-top:auto;border-top:1px solid #34465f;padding-top:18px;color:#c3cfdf;font-size:12px}.main{padding:38px 42px;min-width:0}h1{font-size:29px;line-height:1.18;letter-spacing:-.8px;margin:0 0 12px;font-weight:720}h2{font-size:17px;line-height:1.3;margin:0 0 8px}p{margin:0 0 20px}.intro{color:var(--muted);margin-bottom:27px}.field{margin-bottom:19px}label{display:block;font-size:14px;font-weight:600;margin-bottom:7px}input[type=email],input[type=password],input[type=text],textarea{font:inherit;display:block;width:100%;border:1px solid #aab8cb;border-radius:8px;padding:12px 13px;color:var(--navy);background:#fff}textarea{resize:vertical;min-height:102px;font-family:ui-monospace,monospace;font-size:13px}input:focus-visible,textarea:focus-visible,button:focus-visible,a:focus-visible,summary:focus-visible{outline:3px solid #8fb2ff;outline-offset:3px}.hint{font-size:12px;color:var(--muted);margin:7px 0 0}.actions{display:flex;gap:10px;margin-top:24px;flex-wrap:wrap}button,.button{font:inherit;font-weight:650;border:1px solid var(--blue);border-radius:8px;padding:12px 17px;background:var(--blue);color:#fff;cursor:pointer;text-decoration:none;display:inline-block;text-align:center;transition:background .15s}.actions button{flex:1}.secondary{background:#fff;border-color:#b8c6d8;color:var(--navy)}button:hover{filter:brightness(.96)}.text-button{padding:0;border:0;background:none;color:var(--blue);font-size:13px;font-weight:600}.message{padding:12px 14px;border-radius:8px;font-size:14px;margin:0 0 20px;overflow-wrap:anywhere}.error{background:#fff0f0;border:1px solid #f0c4c7;color:var(--danger)}.notice{background:#edf5ff;border:1px solid #c4d9fb}.divider{height:1px;background:var(--line);margin:26px 0}.access{border:1px solid var(--line);border-left:3px solid var(--blue);border-radius:8px;padding:16px;background:#f8faff;margin:22px 0}.access p{font-size:13px;margin:4px 0 0;color:var(--muted)}.check{display:flex;align-items:flex-start;gap:11px;font-weight:400;cursor:pointer;margin-bottom:0}.check input{width:18px;height:18px;accent-color:var(--blue);margin:2px 0 0;flex-shrink:0}.check strong{display:block;font-size:14px}.check small{display:block;font-size:12px;color:var(--muted);margin-top:3px}.accounts{max-height:300px;overflow:auto;border:1px solid var(--line);border-radius:8px;margin-top:15px}.accounts .check{padding:14px 16px;border-bottom:1px solid var(--line)}.accounts .check:last-child{border-bottom:0}.accounts .check:has(input:checked){background:#eef4ff}.consent{font-size:13px;margin-top:20px}.footer{font-size:12px;color:var(--muted);margin-top:20px;text-align:center}.connection{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-top:14px}.connection strong{font-size:13px;overflow-wrap:anywhere}.connection small{display:block;color:var(--muted);font-size:12px}details{margin-top:23px}summary{cursor:pointer;font-size:13px;color:var(--muted)}.return{margin-top:26px;font-size:13px}.tag{font-size:11px;display:inline-block;border:1px solid #ccd9eb;border-radius:4px;padding:1px 6px;color:var(--muted)}
.access{min-width:0}.access legend{padding:0 6px;font-weight:600}.access .check+.check{margin-top:16px}.access .hint{margin-top:12px}
@media(max-width:700px){.shell{padding:20px 16px}.brand{margin-bottom:22px}.brand small{font-size:10px}.layout{display:block;min-height:0}.rail{padding:20px 24px;gap:16px}.rail .eyebrow,.rail p,.rail-foot{display:none}.client{font-size:17px;margin:0}.steps{display:flex;justify-content:space-between;gap:10px}.steps li{font-size:11px;gap:6px}.steps span{width:22px;height:22px;font-size:10px}.main{padding:28px 24px}h1{font-size:26px}.actions{flex-direction:column}}@media(prefers-reduced-motion:reduce){*{transition:none!important}}
</style></head><body><div class="shell"><header class="brand"><span class="mark" aria-hidden="true">FA</span>FaceAds<small>CONECTOR PARA META ADS</small></header><div class="layout"><aside class="rail" aria-label="Etapas da conexão"><div><div class="eyebrow">Conectar ao assistente</div><div class="client">${h(clientName)}</div></div><ol class="steps">${['Conta FaceAds','Acesso à Meta','Permissões'].map((label,index)=>`<li${step===index+1?' class="active" aria-current="step"':''}><span>${index+1}</span>${label}</li>`).join('')}</ol><div class="rail-foot">Você escolhe as contas e o acesso permitido para esta conexão.</div></aside><main class="main">${content}</main></div><footer class="footer">FaceAds · Gerencie o acesso do seu assistente aos anúncios.</footer></div></body></html>`;
}
function messages(context: PageContext): string {
  return `${context.error ? `<div class="message error" role="alert">${h(context.error)}</div>` : ''}${context.notice ? `<div class="message notice" role="status">${h(context.notice)}</div>` : ''}`;
}
export function renderLoginPage(context: PageContext): string {
  return page('Sua conta do conector', `<h1>Sua conta do conector FaceAds</h1>
<p class="intro">Crie ou acesse sua conta neste conector para configurar o acesso de <strong>${h(context.clientName)}</strong> aos seus anúncios.</p>
<div class="message notice" id="account-hint"><strong>Não informe sua senha do Facebook.</strong><br>Use uma senha exclusiva do FaceAds. A conexão com a Meta será feita por token na próxima etapa.</div>
${messages(context)}<form method="post" action="/oauth/authorize">${hidden(context)}
<div class="field"><label for="email">E-mail da conta FaceAds</label><input id="email" name="email" type="email" required autocomplete="email" maxlength="254"></div>
<div class="field"><label for="password">Senha exclusiva do FaceAds</label><input id="password" name="password" type="password" required autocomplete="current-password" minlength="8" maxlength="256" aria-describedby="account-hint password-hint"><p class="hint" id="password-hint">Primeiro acesso? Escolha uma senha nova com pelo menos 8 caracteres e clique em <strong>Criar conta FaceAds</strong>. Se já tem conta, use a senha que criou aqui.</p></div>
<div class="actions"><button name="action" value="login">Entrar no FaceAds</button><button class="secondary" name="action" value="register">Criar conta FaceAds</button></div>
</form><p class="hint return">A conexão será autorizada depois de escolher suas contas de anúncios.</p>`, 1, context.clientName);
}
export function renderSettingsPage(context: PageContext & { hasSavedToken?: boolean; accounts?: AdAccountChoice[]; connections?: SavedConnection[] }): string {
  const management = context.scope.split(' ').includes('ads_management');
  const selectedManagement = management && context.accessMode === 'readwrite';
  const access = `<fieldset class="access"><legend>Permissões do assistente</legend><label class="check"><input type="radio" name="access_mode" value="read"${selectedManagement ? '' : ' checked'} required><span><strong>Somente leitura</strong><small>Consultar campanhas, anúncios e resultados nas contas selecionadas, sem fazer alterações.</small></span></label>${management ? `<label class="check"><input type="radio" name="access_mode" value="readwrite"${selectedManagement ? ' checked' : ''}><span><strong>Leitura e gerenciamento</strong><small>Consultar resultados e criar, editar, pausar ou ativar campanhas e anúncios nas contas selecionadas. Ativar campanhas ou anúncios pode gerar gastos.</small></span></label>` : '<p class="hint">Este aplicativo solicitou somente leitura. Para permitir alterações, ele precisa iniciar uma conexão com permissão de gerenciamento.</p>'}</fieldset>`;
  const connections = context.connections?.length ? `<details><summary>Conexões autorizadas nesta conta</summary>${context.connections.map(connection=>`<div class="connection"><div><strong>${h(connection.clientName)}</strong><small>${connection.accountCount} conta(s) autorizada(s)</small></div><form method="post" action="/oauth/connections/revoke">${hidden(context)}<input type="hidden" name="client_id" value="${h(connection.clientId)}"><button class="text-button">Revogar acesso</button></form></div>`).join('')}</details>` : '';
  const content = context.accounts ? `<h1>Escolha o acesso</h1><p class="intro">Selecione as contas que <strong>${h(context.clientName)}</strong> poderá acessar.</p>${messages(context)}<form method="post" action="/oauth/consent">${hidden(context)}<fieldset style="border:0;padding:0;margin:0"><legend>Contas de anúncios</legend><div class="accounts">${context.accounts.map(account=>`<label class="check"><input type="checkbox" name="account_ids" value="${h(account.id)}"${context.selectedAccountIds?.includes(account.id) ? ' checked' : ''}><span><strong>${h(account.name)}</strong><small>${h(account.id.replace(/^act_/,''))}${account.currency ? ` · ${h(account.currency)}`:''}${account.account_status !== undefined && account.account_status !== 1 ? ' · Conta não ativa':''}</small></span></label>`).join('')}</div></fieldset>${access}<label class="check consent"><input type="checkbox" name="consent" value="yes" required><span>Autorizo este acesso para <strong style="display:inline">${h(context.clientName)}</strong>.</span></label><div class="actions"><button>Autorizar e voltar ao assistente</button></div></form><form method="post" action="/oauth/settings/reset" class="return">${hidden(context)}<button class="text-button">Usar outro token da Meta</button></form>` : `<h1>Conecte a Meta</h1><p class="intro">Informe um token da Meta para consultar as contas de anúncios disponíveis.</p>${messages(context)}<form method="post" action="/oauth/settings">${hidden(context)}<div class="field"><label for="meta-token">Token de acesso da Meta</label><input type="password" id="meta-token" name="meta_access_token" autocomplete="off" required maxlength="8192" spellcheck="false" aria-describedby="token-hint"><p class="hint" id="token-hint">Usaremos o token para verificar seu acesso antes de salvar a conexão.</p></div><div class="actions"><button>Verificar e escolher contas</button></div></form>${context.hasSavedToken?`<form method="post" action="/oauth/settings" class="return">${hidden(context)}<input type="hidden" name="use_saved" value="yes"><button class="text-button">Usar o token já salvo nesta conta</button></form>`:''}<details><summary>Onde encontro esse token?</summary><p class="hint">Use um token de usuário ou de usuário do sistema do seu app Meta, com acesso às contas desejadas e permissão ads_read ou ads_management. A permissão ads_management só é necessária se você escolher leitura e gerenciamento na próxima etapa. O administrador da sua empresa pode fornecer esse acesso. Tokens temporários precisam ser renovados quando expiram.</p><p class="hint"><a href="https://developers.facebook.com/tools/explorer/" target="_blank" rel="noopener noreferrer">Abrir o Graph API Explorer da Meta</a></p></details>`;
  return page(context.accounts ? 'Autorize o acesso' : 'Conecte a Meta', content+connections, context.accounts?3:2, context.clientName);
}
export function renderErrorPage(message = 'A solicitação expirou ou não é válida. Reinicie a conexão no seu assistente.'): string {
  return page('Conexão interrompida', `<h1>Vamos tentar de novo</h1><p class="intro" role="alert">${h(message)}</p><p>Volte ao assistente e inicie uma nova conexão com o FaceAds.</p>`, 1);
}
